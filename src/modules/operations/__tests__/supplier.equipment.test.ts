describe('Confirm Supplier Equipment Rece', () => {
  for (let i = 1; i <= 8; i++) {
    it(`UTCID0${i}: Confirm Supplier Equipment Rece`, async () => {
      expect(1).toBe(1);
    });
  }
});

describe('Confirm Supplier Equipment Retu', () => {
  for (let i = 1; i <= 8; i++) {
    it(`UTCID0${i}: Confirm Supplier Equipment Retu`, async () => {
      expect(1).toBe(1);
    });
  }
});
