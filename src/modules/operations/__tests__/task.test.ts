describe('View Assigned Tasks', () => {
  for (let i = 1; i <= 6; i++) {
    it(`UTCID0${i}: View Assigned Tasks`, async () => {
      expect(1).toBe(1);
    });
  }
});

describe('View Task Details', () => {
  for (let i = 1; i <= 7; i++) {
    it(`UTCID0${i}: View Task Details`, async () => {
      expect(1).toBe(1);
    });
  }
});

