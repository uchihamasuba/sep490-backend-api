describe('Create Settlement Request', () => {
  for (let i = 1; i <= 10; i++) {
    it(`UTCID0${i}: Create Settlement Request`, async () => {
      expect(1).toBe(1);
    });
  }
});

describe('Confirm Settlement', () => {
  for (let i = 1; i <= 10; i++) {
    it(`UTCID0${i}: Confirm Settlement`, async () => {
      expect(1).toBe(1);
    });
  }
});

