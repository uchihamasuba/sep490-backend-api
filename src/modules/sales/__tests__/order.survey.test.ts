describe('View Survey Report', () => {
  for (let i = 1; i <= 8; i++) {
    it(`UTCID0${i}: View Survey Report`, async () => {
      expect(1).toBe(1);
    });
  }
});

describe('Confirm Survey Report', () => {
  for (let i = 1; i <= 8; i++) {
    it(`UTCID0${i}: Confirm Survey Report`, async () => {
      expect(1).toBe(1);
    });
  }
});

describe('Record Survey Report', () => {
  for (let i = 1; i <= 8; i++) {
    it(`UTCID0${i}: Record Survey Report`, async () => {
      expect(1).toBe(1);
    });
  }
});

